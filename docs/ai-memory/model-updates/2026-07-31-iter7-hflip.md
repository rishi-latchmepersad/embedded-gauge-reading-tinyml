# Iteration 7: hflip augmentation fixes test_2 — test_1 is the last gate — 2026-07-31

Date: 2026-07-31
Status: current
Scope: ellipse detector iterations (target <10px center MAE on all 3 test sets)
Evidence: `ml/artifacts/ellipse_iter7_universal_hflip/report.json`

## Results (int8, 30,050 samples, tiny200 + 50% random hflip augmentation)

| Split | iter6 | **iter7 (hflip)** | gate |
|---|---|---|---|
| test_1 center MAE | 12.55px | 12.67px | ✗ <10px |
| test_2 center MAE | 28.03px | **6.27px** | ✓ <10px |
| test_3 center MAE | 6.02px | **6.10px** | ✓ <10px |

## What worked

The 50% random horizontal flip in `_augment_uint8` (cx mirrored exactly)
fixed test_2: the hflip IMG_144x variant was failing at 178px because no
flipped views reached training. Now 82% of test_2 within 8px, median 4.0px.
This is the WACV-2024-style flip/rotate augmentation lesson applied.

## Remaining gate

test_1 (914 generic pressure gauges) is stuck at ~12.7px across iter4/6/7.
Its median is 9.0px and 45% ≤8px — the tail is dominated by a subset of
hard/elongated gauges (rx:ry ratio up to 8:1 per the crop-pipeline note).

## Next

1. Screen #3: 10-architecture roster at ~5 min/arch (14 epochs, per-arch
   seed reset) to settle the architecture question properly before more
   full runs. Launcher: `tmp/run_screen_fast.sh`.
2. For test_1 specifically: the augmentation already flips/translates;
   candidate fixes are elongated-ellipse-aware sampling or a wider encoder
   (the screen will show if width/attention helps).
